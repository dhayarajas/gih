# Runtime dependencies. Everything here is imported unconditionally by src/.
# Dev tooling lives in requirements-dev.txt; heavy opt-in extras (face matching,
# Neo4j backend) live in requirements-optional.txt.
click>=8.1.0
networkx>=3.0
phonenumbers>=8.13.0
requests>=2.31.0
# Search engines serve brotli-encoded HTML; without a decoder responses arrive
# as undecodable bytes and every scraper silently returns nothing.
Brotli>=1.1.0
beautifulsoup4>=4.12.0
Pillow>=10.0.0
pyvis>=0.3.2
Jinja2>=3.1.0
tabulate>=0.9.0
pyyaml>=6.0.0
numpy>=1.24.0
urllib3>=2.0.0
